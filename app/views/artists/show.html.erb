<h1><%= @artist.name %></h1>

<%= link_to '[all artists]', artists_path %>
<ul class='block'>
	<li class='all'><%= link_to "All #{@artist.songs.count} Songs", artist_songs_path(@artist) %></li>

	<%= render @albums %>
</ul>
