<%= form_for [@artist, @album], :remote => true do |f| %>
  <div>
    <%= f.text_field :name %>
    <%= f.submit 'Submit', :disable_with => 'Submitting' %>
    <%= link_to '[cancel]', [@artist, @album], :remote => true %>
  </div>
<% end %>
